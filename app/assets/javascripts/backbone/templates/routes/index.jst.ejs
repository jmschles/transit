<ul>
  <% routes.each(function(route) { %>
    <% var routeId = route.get('id'); %>
    <li class="route_name" route_id="<%= routeId %>" id="route_<%= routeId %>"><%= route.get('name') %></li>
  <% }); %>
</ul>